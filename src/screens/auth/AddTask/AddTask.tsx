import { FC } from 'react'
import { Text } from 'react-native'

import { styles } from './AddTask.styles'

interface AddTaskProps {}

const AddTask: FC<AddTaskProps> = () => {
  return <Text>Add task</Text>
}

export default AddTask
