import { configStyles } from '@configs/style'
import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  text: {
    fontWeight: '300',
    fontSize: 24,
    lineHeight: 28,
    color: configStyles.colors.text.title,
  },
})
